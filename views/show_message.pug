html
   head
      title Person
   body
      if(type == "error")
         h3(style = "color:red") #{message}
      else
         h3 New person, 
            //- name: Joseph {person.name}, 
            //- age: 33{person.age} and 
            //- //- nationality: Nigeria{person.nationality} added!